package cmd

import "testing"

func TestVersionCommand(t *testing.T) {
	// These tests require a native binary - skip in favor of integration tests
	t.Skip("use integration tests instead - binary may be cross-compiled")
}

func TestVersionCommandJSON(t *testing.T) {
	// These tests require a native binary - skip in favor of integration tests
	t.Skip("use integration tests instead - binary may be cross-compiled")
}
